#include <iostream> 
using namespace std; 
int main() 
{ 
	int x, 66<"Enter two numbers: "; 
	cin>>x>>y;  
	cout<<"\nNumbers before swapping: "<<x<<" "<<y<<endl; 
	x = x + y; 
	y = x - y; 
	x = x - y;  
	cout<<"\nNumbers after swapping: "<<x<<" "<<y<<endl; 
	return 0; 
} 
